{% extends "SnowTricksHomeBundle::layout.html.twig" %}

{% block body %}
    <div class="container" id="two">
        <div class="row">
            <div class="col-lg-8 col-md-10 mx-auto">
                <h2 class="post-title">Mot de passe oublié</h2>
                {{ form_start(form) }}

                    {{ form_row(form.email, {
                        'label': 'Renseignez votre email'
                    }) }}
                    
                    <button type="submit" class="btn btn-primary" formnovalidate>Demandez un nouveau mot de passe</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}