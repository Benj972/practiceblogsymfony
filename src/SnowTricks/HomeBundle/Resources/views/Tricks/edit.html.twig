{% extends "SnowTricksHomeBundle::layout.html.twig" %}

{% block body %}

    <div class="container" id="two">
        <div class="row">
            <div class="col-lg-8 col-md-10 mx-auto">
                <h2 class="post-title">{{ title }}</h2>

                    <!-- Modal -->
                    <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                        <div class="modal-dialog" role="document">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title" id="exampleModalLabel">EXPLICATIONS</h5>
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                    </button>
                                </div>
                                <div class="modal-body">
                                    <p>Vous pouvez modifier, ajouter et supprimer plusieurs images et videos.</p>
                                    <p>Pour les videos copier le code d'intégration d'une vidéo et garder seulement le lien URL https://www.youtube.com/embed/n0F6hSpxaF</p>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermez</button>
                                </div>
                            </div>
                        </div>
                    </div>


                {{ include("SnowTricksHomeBundle:Tricks:form.html.twig") }}

                <p>
                    Vous éditez une figure déjà existante, merci de ne pas changer
                    l'esprit générale de la figure déjà publiée.
                </p>

                {% if trick.slug %}
                    <p>
                        <a href="{{ path('snow_tricks_home_view', {'slug': trick.slug}) }}" class="btn btn-default">
                            <i class="glyphicon glyphicon-chevron-left"></i>
                            Retour à la figure
                        </a>
                    </p>
                {% endif %}
            </div>
        </div>
    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}